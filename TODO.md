# SnapSphere Backend — Development TODO

> Complete each task before moving to the next. New tasks will be added as we go.

---

- [ ] **Task 1 — Project Setup & Folder Structure**
  - Initialize Express server
  - Create folder structure (config, controllers, models, routes, middlewares, utils)
  - Install dependencies (express, mongoose, dotenv, cors, helmet, etc.)
  - Create `.env` file with environment variables
  - Create `server.js` entry point with basic Express app
  - Add dev script with nodemon
